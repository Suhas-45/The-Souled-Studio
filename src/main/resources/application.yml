spring:
  datasource:
    url: jdbc:mysql://localhost:3306/the_souled_studio_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Suhas@3194

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8081
